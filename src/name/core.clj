(ns name.core
  (:require [clj-http.client :as client]
            [clojure.string :as str]
            [quil.core :as q]
            [quil.middleware :as m]
            [cheshire.core :as cheshire]))

(inc 1)
